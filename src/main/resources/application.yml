spring:
  application:
    name: taskFlow
  datasource:
    url: ${url}
    username: ${root}
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret-key: "S2VrMjlmanNkXzEhYUpkbGxmMDNzZGZRdzhYbnp2MDkmREZhMjl3QHNhTGs="
  bearer-prefix: "Bearer "

